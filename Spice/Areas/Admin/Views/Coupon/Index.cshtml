@model IEnumerable<Coupon>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br /><br />

<div class="border backgroundwhite">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info">Coupon List</h2>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreatePartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {

            <table class="table table-striped border">
                <tr>
                    <th>
                        @Html.DisplayNameFor(m => m.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.CouponType)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.Discount)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.isActive)
                    </th>
                </tr>
                @foreach (var item in Model.ToList())
                {
                <tr>
                    <td>
                        @Html.DisplayFor(m => item.Name)
                    </td>
                    <th>
                        
                        @Enum.GetName(typeof(Coupon.ECouponType), Convert.ToInt32(item.CouponType))

                    </th>
                    <th>
                        @Html.DisplayFor(m => item.Discount)
                    </th>
                    <th>
                        @Html.DisplayFor(m => item.isActive)
                    </th>
                    <td>
                        <partial name="_CreateDetailDeletePartial" model="item.Id" />
                    </td>
                   

                </tr>
                }
            </table>
        }
        else
        {
            <p>
                No category exists.
            </p>
        }
    </div>

</div>


